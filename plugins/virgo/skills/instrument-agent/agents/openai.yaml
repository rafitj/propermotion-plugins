interface:
  display_name: "Instrument Agent"
  short_description: "Design and ship agent observability"
  default_prompt: "Use $virgo:instrument-agent to propose comprehensive observability, then let me choose the exact edits."
policy:
  allow_implicit_invocation: true
